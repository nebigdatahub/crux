#   A basic travis.yml boilerplate for Django projects
#
#   The set-up assumes a postgreSQL database.
#
#   Replace the following variables in the code:
# * your_project_settings
# * your_github_username
# * your_repo

language: python

python:
  - 3.6

services: postgresql

env:
  - DJANGO=1.4.1

before_install:
  - export DJANGO_SETTINGS_MODULE=crux.settings
  - export PYTHONPATH=$HOME/nebigdatahub/crux
  - export PIP_USE_MIRRORS=true
  - pip install -U pip
  - export PYTHONPATH=$PYTHONPATH:$(pwd)

install:
  - pip install pipenv
  - pip install -r requirements.txt
  - server/manage.py migrate
  - server/manage.py loaddata users datasets analyses files

after_success:
  - coveralls

script:
  - pylint server/
  - server/manage.py runserver
  - nosetests


notifications:
  email:
    - raphnano@gmail.com
    - acm2248@columbia.edu