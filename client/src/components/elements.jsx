import React, { Component } from "react"

const Title = ({ text }) => <h1 className="title">{text}</h1>

const Subtitle = ({ text }) => <h2 className="subtitle">{text}</h2>

export { Title, Subtitle }
